/**
 * @butai/library-web — GSAP recipe library (data lives in `recipes/`, generated
 * catalog in `catalog/`, runnable demos in `demos/`). This module only exposes
 * package constants; the catalog JSON is the machine interface.
 */
export const LIBRARY_WEB = '@butai/library-web';
export const CATALOG_JSON = 'catalog/catalog.json';
export const CATALOG_MD = 'catalog/catalog.md';
