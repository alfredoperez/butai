{
  "name": "@butai/skills-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private workspace carrying the butai-skills plugin manifest + validation and forbidden-strings tests.",
  "scripts": {
    "build": "echo \"@butai/skills-plugin: no build output (skills are docs)\"",
    "lint": "eslint src --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.0",
    "eslint": "^9.15.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.15.0",
    "vitest": "^2.1.0",
    "yaml": "^2.6.0"
  }
}
