/**
 * CoverSlide — the deck opener. Big left-aligned display-font title, eyebrow,
 * subtitle, and an optional faded background image. Re-exports the shared
 * IntroSlide primitive so the kit and real decks stay in sync.
 */
import { IntroSlide } from "../primitives/intro-slide.js";

export { IntroSlide as CoverSlide };
