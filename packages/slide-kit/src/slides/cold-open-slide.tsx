/**
 * ColdOpenSlide — same lockup as the cover, but flagged as a cold open: styling
 * holds the title back so it lands on the first click for impact. Backed by the
 * shared IntroSlide primitive (pass `coldOpen`).
 */
import { IntroSlide } from "../primitives/intro-slide.js";

export { IntroSlide as ColdOpenSlide };
